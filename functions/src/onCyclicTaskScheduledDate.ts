// import {firestore} from "firebase-admin";

// export const onCyclicTaskScheduledDate = (snapshot) => {
//   return firestore()
//   .collection("tasks")
//   .add({...snapshot, beginningDate: new Date()}); // TODO: Calculate future date and create future task occurence
// };
